import React from 'react'; import { Card,Button } from '../ui'; export function OnboardingChecklist({steps,onNavigate,onDismiss}:any){return <Card className="p-4"><div className="mb-3 flex items-center justify-between"><h3 className="font-semibold">Workspace checklist</h3>{onDismiss&&<button onClick={onDismiss} className="text-xs text-gib-muted">Dismiss</button>}</div><div className="space-y-2">{steps.map((s:any)=><div key={s.id} className="flex items-center justify-between"><span className={s.done?'text-gib-muted line-through':''}>{s.label}</span><Button size="sm" variant="secondary" onClick={()=>onNavigate(s.tab,s.subTab)}>{s.done?'Done':'Open'}</Button></div>)}</div></Card>}
