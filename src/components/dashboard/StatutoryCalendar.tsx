'use client'; import React from 'react'; import { Card,Button } from '../ui'; export function StatutoryCalendar({events,onAddDeadline}:any){return <Card className="p-4"><div className="mb-3 flex items-center justify-between"><h3 className="font-semibold">Statutory calendar</h3><Button size="sm" onClick={()=>onAddDeadline?.()}>Add deadline</Button></div><div className="space-y-2">{events.slice(0,6).map((e:any)=><div key={e.id} className="flex items-center justify-between border-b border-gib-border py-2 last:border-0"><div><div className="font-medium">{e.title}</div><div className="text-xs text-gib-muted">{e.description}</div></div><span className="text-xs font-mono text-gib-muted">{e.date}</span></div>)}</div></Card>}
