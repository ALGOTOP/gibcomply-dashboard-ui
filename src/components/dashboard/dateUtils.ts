export function parseISODate(s:string){return new Date(s+'T00:00:00')} export function formatShortDate(d:Date){return d.toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric'})} export function formatLongDay(d:Date){return d.toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'long'})} export function formatMonthYear(d:Date){return d.toLocaleDateString('en-GB',{month:'long',year:'numeric'})} export function toISODate(d:Date){return d.toISOString().slice(0,10)} export function addDays(d:Date,n:number){const x=new Date(d);x.setDate(x.getDate()+n);return x} export function addMonths(d:Date,n:number){const x=new Date(d);x.setMonth(x.getMonth()+n);return x} export function isSameDay(a:Date,b:Date){return a.toDateString()===b.toDateString()} export function startOfWeek(d:Date){const x=new Date(d);x.setDate(x.getDate()-x.getDay()+1);return x} export function formatWhen(v:string){return new Date(v).toLocaleDateString('en-GB',{day:'2-digit',month:'short'})}
