import type { CalendarEvent } from '../../types/compliance'; import type { Tone } from '../../lib/ui/tone'; export type EventStatus='overdue'|'verified'|'milestone'|'neutral'; export interface DashboardEvent{id:string;title:string;description:string;date:string;status:EventStatus;chip:string;chipShort:string;reference:string;location:string|null;tags:string[];attendees:string[];autoTracked:boolean;completed:boolean;source:CalendarEvent;} export interface OnboardingStep{id:string;label:string;done:boolean;tab:string;subTab?:'corporate'|'personal'} export interface FirstRunItem{id:string;label:string;state:string;next:string;tab:string;subTab?:'corporate'|'personal';actionLabel:string} export interface Checkpoint{id:string;label:string;detail:string;done:boolean;tab:string;subTab?:'corporate'|'personal';advisory?:boolean} export interface Readiness{percent:number;tone:Tone;done:number;total:number} export interface StatChip{label:string;tone:Tone} export interface DashboardStat{id:string;label:string;value:number;chip:StatChip;caption:string;tab:string;subTab?:'corporate'|'personal'} export type TrailKind='overdue'|'insert'|'update'|'delete'; export interface TrailEntry{id:string;kind:TrailKind;title:string;at:string;actor:string} export interface WatchlistRow{id:string;name:string;scope:string;classification:StatChip;status:{label:string;tone:Tone};dueDiligence:StatChip;flagged:boolean}
