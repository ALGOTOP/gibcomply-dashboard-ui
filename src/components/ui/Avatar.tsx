import React from 'react'; import { cn } from '@/lib/cn'; export function Avatar({name,size='md',className}:{name:string,size?:'sm'|'md'|'lg',className?:string}){const s=size==='sm'?'h-7 w-7':size==='lg'?'h-10 w-10':'h-8 w-8'; return <span className={cn('inline-flex items-center justify-center rounded-full bg-gib-hover text-gib-text text-xs font-medium',s,className)}>{name?.trim()?.[0]?.toUpperCase()||'?'}</span>}
