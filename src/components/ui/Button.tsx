import React from 'react'; import Link from 'next/link'; import { cn } from '@/lib/cn'; export function Button({children,href,onClick,className,variant='primary',size='md',icon:Icon,disabled,loading,title,shape,...rest}:any){const cls=cn('inline-flex items-center justify-center gap-2 rounded-md font-medium transition-colors disabled:opacity-50',size==='sm'?'h-8 px-3 text-xs':'h-9 px-4 text-sm',variant==='secondary'?'border border-gib-border bg-white text-gib-text hover:bg-gib-hover':'bg-gib-brand text-white hover:bg-gib-brandStrong',shape==='pill'&&'rounded-full',className); const body=<>{Icon&&<Icon className="h-4 w-4"/>}{loading?'Loading…':children}</>; return href?<Link href={href} className={cls} title={title}>{body}</Link>:<button className={cls} onClick={onClick} disabled={disabled||loading} title={title} {...rest}>{body}</button>}
