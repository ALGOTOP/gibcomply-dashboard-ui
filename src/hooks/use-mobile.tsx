'use client'; import * as React from 'react'; const MOBILE_BREAKPOINT=768; export function useIsMobile(){ const [isMobile,setIsMobile]=React.useState(false); React.useEffect(()=>{const m=window.matchMedia(`(max-width: ${MOBILE_BREAKPOINT-1}px)`); const on=()=>setIsMobile(window.innerWidth<MOBILE_BREAKPOINT); m.addEventListener('change',on); on(); return()=>m.removeEventListener('change',on);},[]); return isMobile; }
